// a function to convert text to OED house style

function style(string) {
    //replace quots
    //replace dashes
    //capitalise first
    //fullstop at end if no other punctuation
    //get rid of double spaces from OCR
}